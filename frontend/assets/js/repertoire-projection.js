if(document.documentElement.dataset.page==='repertoires'){new MutationObserver(async()=>{const t=[...document.querySelectorAll('#app table')].find(x=>x.querySelector('th')?.textContent.trim()==='Culto');if(!t||t.dataset.projection)return;t.dataset.projection=1;const rs=await API.get('/repertoires');[...t.querySelectorAll('tbody tr')].forEach((row,i)=>{const r=rs[i],cell=row.lastElementChild;if(r&&!cell.querySelector('.start-projection'))cell.insertAdjacentHTML('beforeend',` <button class="btn btn-sm btn-dark start-projection" data-id="${r.id}"><i class="bi bi-projector"></i> Projetar</button>`)});}).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{const b=event.target.closest('.start-projection');if(!b)return;const base=location.origin,projector=`${base}/pages/projection.html?repertoire=${b.dataset.id}&v=3`,control=`${base}/pages/projection-control.html?repertoire=${b.dataset.id}`,login=`${base}/login?next=${encodeURIComponent('/pages/projection-control.html?repertoire='+b.dataset.id)}`,qr=`https://api.qrserver.com/v1/create-qr-code/?size=240x240&data=${encodeURIComponent(login)}`;window.open(projector,'projection');const box=document.createElement('div');box.className='modal fade';box.tabIndex=-1;box.innerHTML=`<div class="modal-dialog modal-dialog-centered"><div class="modal-content text-center"><div class="modal-header"><h2 class="modal-title fs-5">Controle pelo celular</h2><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><p>Aponte a câmera para o QR Code. Após entrar, seu celular abrirá o controle.</p><img src="${qr}" width="240" height="240" alt="QR Code para controle da projeção"><p class="small text-break mt-3"><a href="${login}" target="_blank">Abrir controle manualmente</a></p></div></div></div>`;document.body.append(box);const modal=new bootstrap.Modal(box);box.addEventListener('hidden.bs.modal',()=>box.remove(),{once:true});modal.show()});}
