if(document.documentElement.dataset.page==='generate')new MutationObserver(()=>{const form=document.querySelector('#gf');if(!form||form.dataset.polished)return;form.dataset.polished='1';const card=form.closest('.card');card.classList.add('planning-form');card.insertAdjacentHTML('beforebegin','<section class="planning-intro"><div><h2>Planeje o culto com clareza</h2><p>Organize músicas, liturgia e apresentação em um único fluxo.</p></div><i class="bi bi-journal-check"></i></section>');[['hymns','Quantidade de hinos'],['upbeat','Louvores animados'],['calm','Louvores calmos']].forEach(([name,label])=>{const input=form.querySelector(`[name="${name}"]`);if(!input)return;input.parentElement.insertAdjacentHTML('afterbegin',`<label class="form-label">${label}</label>`)});}).observe(document.documentElement,{childList:true,subtree:true});
