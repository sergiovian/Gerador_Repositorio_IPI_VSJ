if(document.documentElement.dataset.page==='repertoires'){async function order(id){const repertoire=await API.get(`/repertoires/${id}`),items=[...repertoire.items];const box=document.createElement('div');box.className='modal fade';box.tabIndex=-1;const draw=()=>{box.innerHTML=`<div class="modal-dialog modal-dialog-scrollable"><form class="modal-content"><div class="modal-header"><div><h2 class="modal-title fs-5">Definir ordem do repertório</h2><small class="text-secondary">A ordem salva será usada no PDF e no modo Apresentar.</small></div><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><div class="list-group">${items.map((item,index)=>`<div class="list-group-item d-flex align-items-center gap-2"><strong class="text-secondary">${index+1}</strong><div class="flex-grow-1"><strong>${UI.esc(item.title)}</strong><br><small class="text-secondary">${UI.esc(item.artist_name||'')} · ${UI.esc(item.role)}</small></div><div class="btn-group"><button type="button" class="btn btn-sm btn-outline-secondary move-up" data-index="${index}" ${index?'':'disabled'}><i class="bi bi-arrow-up"></i></button><button type="button" class="btn btn-sm btn-outline-secondary move-down" data-index="${index}" ${index<items.length-1?'':'disabled'}><i class="bi bi-arrow-down"></i></button></div></div>`).join('')}</div></div><div class="modal-footer"><button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button><button class="btn btn-primary" type="submit"><i class="bi bi-save me-1"></i>Salvar ordem</button></div></form></div>`;box.querySelectorAll('.move-up').forEach(button=>button.onclick=()=>{const i=Number(button.dataset.index);[items[i-1],items[i]]=[items[i],items[i-1]];draw()});box.querySelectorAll('.move-down').forEach(button=>button.onclick=()=>{const i=Number(button.dataset.index);[items[i],items[i+1]]=[items[i+1],items[i]];draw()});box.querySelector('form').onsubmit=async event=>{event.preventDefault();const submit=box.querySelector('[type="submit"]');submit.disabled=true;try{await API.put(`/repertoires/${id}/items`,items.map((item,index)=>({music_id:item.music_id,position:index+1,role:item.role,score:item.score,reasons:JSON.parse(item.reasons_json||'[]'),warnings:JSON.parse(item.warnings_json||'[]')})));modal.hide();UI.alert('Ordem do repertório salva.');location.reload()}catch(error){UI.alert(error.message,'danger');submit.disabled=false}}};draw();document.body.append(box);const modal=new bootstrap.Modal(box);box.addEventListener('hidden.bs.modal',()=>box.remove(),{once:true});modal.show()}new MutationObserver(async()=>{const table=[...document.querySelectorAll('#app table')].find(t=>t.querySelector('th')?.textContent.trim()==='Culto');if(!table||table.dataset.orderReady)return;table.dataset.orderReady='1';const records=await API.get('/repertoires');[...table.querySelectorAll('tbody tr')].forEach((row,index)=>{const record=records[index],cell=row.lastElementChild;if(record&&['DRAFT','CONFIRMED'].includes(record.status)&&!cell.querySelector('.order-repertoire'))cell.insertAdjacentHTML('afterbegin',`<button class="btn btn-sm btn-outline-secondary order-repertoire me-1" data-id="${record.id}"><i class="bi bi-list-ol"></i> Ordenar</button>`)});}).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{const button=event.target.closest('.order-repertoire');if(button)order(button.dataset.id)});}
