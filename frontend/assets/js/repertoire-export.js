if(document.documentElement.dataset.page==='repertoires')new MutationObserver(async()=>{const table=[...document.querySelectorAll('#app table')].find(item=>item.querySelector('th')?.textContent.trim()==='Culto');if(!table||table.dataset.exportReady)return;table.dataset.exportReady='true';const repertoires=await API.get('/repertoires');[...table.querySelectorAll('tbody tr')].forEach((row,index)=>{const repertoire=repertoires[index];if(!repertoire)return;row.lastElementChild.insertAdjacentHTML('beforeend',` <button class="btn btn-sm btn-outline-success export-repertoire" data-id="${repertoire.id}"><i class="bi bi-file-earmark-spreadsheet"></i> Excel</button>`);});}).observe(document.documentElement,{childList:true,subtree:true});
document.addEventListener('click',async event=>{const button=event.target.closest('.export-repertoire');if(!button)return;const repertoire=await API.get('/repertoires/'+button.dataset.id),escape=value=>`"${String(value??'').replaceAll('"','""')}"`,rows=[['Data do culto','Status','Posição','Função','Música','Artista'],...repertoire.items.map(item=>[repertoire.service_date,repertoire.status,item.position,item.role,item.title,item.artist_name||''])],csv='\uFEFF'+rows.map(row=>row.map(escape).join(';')).join('\r\n'),blob=new Blob([csv],{type:'text/csv;charset=utf-8'}),link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=`repertorio-${repertoire.service_date}.csv`;link.click();URL.revokeObjectURL(link.href);});
