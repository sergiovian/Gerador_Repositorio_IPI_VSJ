document.addEventListener('DOMContentLoaded',()=>{Layout.render('tuner','Afinador de violão');app=document.querySelector('#app');app.innerHTML='<div class="card panel p-4 text-center"><p class="text-secondary">Use o microfone para afinar em E A D G B E.</p><div id="note" class="display-1 fw-bold text-danger">—</div><div id="freq" class="fs-4 mb-4">Toque uma corda</div><button id="start" class="btn btn-primary">Ativar microfone</button></div>';start.onclick=async()=>{const stream=await navigator.mediaDevices.getUserMedia({audio:true}),ctx=new AudioContext(),source=ctx.createMediaStreamSource(stream),analyser=ctx.createAnalyser(),data=new Float32Array(analyser.fftSize);source.connect(analyser);start.disabled=true;const tick=()=>{analyser.getFloatTimeDomainData(data);let crossings=0;for(let i=1;i<data.length;i++)if(data[i-1]<0&&data[i]>=0)crossings++;const hz=crossings*ctx.sampleRate/data.length;if(hz>70&&hz<500){const midi=Math.round(69+12*Math.log2(hz/440)),names=['C','C♯','D','D♯','E','F','F♯','G','G♯','A','A♯','B'];note.textContent=names[(midi+120)%12];freq.textContent=hz.toFixed(1)+' Hz'}requestAnimationFrame(tick)};tick()}});
