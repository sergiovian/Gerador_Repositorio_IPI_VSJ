/**
 * Contexto temporário da versão 1.0, que atende apenas uma igreja.
 * A autenticação futura substituirá este valor pelo church_id do usuário.
 */
const CURRENT_CHURCH_ID = 1;

module.exports = { CURRENT_CHURCH_ID };
