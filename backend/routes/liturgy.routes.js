const express=require('express');const multer=require('multer');const asyncHandler=require('../utils/async-handler');const Service=require('../services/liturgy-import.service');const r=express.Router(),upload=multer({storage:multer.memoryStorage(),limits:{fileSize:50*1024*1024}});r.post('/import/preview',upload.single('file'),asyncHandler(async(req,res)=>res.json({data:Service.preview(req.file)})));module.exports=r;
