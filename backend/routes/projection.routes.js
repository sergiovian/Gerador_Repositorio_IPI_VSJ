const express=require('express');const db=require('../models/db.model');const {getCurrentChurchId}=require('../constants/church-context');const AppError=require('../utils/app-error');const r=express.Router();const id=x=>{x=Number(x);if(!Number.isInteger(x)||x<1)throw new AppError('Repertório inválido.',400);return x};async function repertoire(x){const row=await db.get('SELECT id FROM repertoires WHERE id=? AND church_id=?',[id(x),getCurrentChurchId()]);if(!row)throw new AppError('Repertório não encontrado.',404);return row.id}r.get('/:id/state',async(req,res,next)=>{try{const rid=await repertoire(req.params.id),row=await db.get('SELECT current_position,blackout FROM projection_states WHERE repertoire_id=?',[rid]);res.json({data:{position:row?.current_position||1,blackout:!!row?.blackout}})}catch(e){next(e)}});r.put('/:id/state',async(req,res,next)=>{try{const rid=await repertoire(req.params.id),position=Math.max(1,Number(req.body.position)||1),blackout=req.body.blackout?1:0;await db.run('INSERT INTO projection_states(repertoire_id,current_position,blackout,updated_at) VALUES(?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(repertoire_id) DO UPDATE SET current_position=excluded.current_position,blackout=excluded.blackout,updated_at=CURRENT_TIMESTAMP',[rid,position,blackout]);res.json({data:{position,blackout:!!blackout}})}catch(e){next(e)}});module.exports=r;
